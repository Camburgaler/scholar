import { JSX } from "react/jsx-runtime";

export default function ReinforcementDisplay(): JSX.Element {
    return (
        <select style={{ backgroundColor: "var(--secondary)" }}>
            {/* TODO: Add reinforcement options */}
            <option>+10</option>
        </select>
    );
}
